export function clamp(value:number, min:number, max:number) {
    return Math.min(Math.max(value, min), max);
}

/// assumes t to be inbetween 0 and 1
export function squareLerp(a:number,b:number,c:number,d:number,t:number) {
    return (a)
}